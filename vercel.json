{
  "build": {
    "env": {
      "NODE_OPTIONS": "--max-old-space-size=6144"
    }
  },
  "buildCommand": "npx prisma generate && node scripts/prisma-migrate-deploy-retry.mjs && npx next build --webpack"
}
