# Stitch exports

Run from the repo root (after `STITCH_API_KEY` is in `.env.local`):

```bash
npm run stitch:fetch
```

Files are written under `design/stitch/<projectId>/`. See [`docs/STITCH_UI.md`](../docs/STITCH_UI.md).
